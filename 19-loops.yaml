- name: install packages
  hosts: web
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.package:
      name: "{{ item.name }}"
      state: "{{ item.state }}"
    loop:
    - { name: 'gcc', state: 'pre sent' }
    - { name: 'git', state: 'present' }
    - { name: 'mysql', state: 'absent' }
    - { name: 'zip', state: 'absent' }
